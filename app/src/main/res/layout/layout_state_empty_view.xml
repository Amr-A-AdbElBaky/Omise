<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/containerStateEmptyView"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextView
        android:id="@+id/tvEmptyItems"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/no_internet_tv_horizontal_margin"
        android:layout_marginEnd="@dimen/no_internet_tv_horizontal_margin"
        android:gravity="center"
        android:fontFamily="@font/font_regular"
        android:textColor="@color/colorDoveGray"
        android:textSize="17sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="You don’t have pending questions. Once, you submit a question,you will be able to view it here." />
</androidx.constraintlayout.widget.ConstraintLayout>